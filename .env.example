# AI API (used by server only — never exposed to frontend)
# AI_PROVIDER=openrouter
# OPENROUTER_API_KEY=your-openrouter-key
# OPENROUTER_MODEL=x-ai/grok-4.1-fast

# Optional: Gemini (if AI_PROVIDER=gemini)
# GEMINI_API_KEY=your-gemini-key

# Server port for AI API (default 3001)
# PORT=3001

# Optional: for production, point frontend to your API base URL
# VITE_API_URL=https://your-api.example.com
